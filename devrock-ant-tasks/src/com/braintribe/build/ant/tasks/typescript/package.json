{
  "name": "${FULL_PACKAGE_NAME}",
  "version": "${VERSION}",
  "main": "dist/${ARTIFACT_ID}.js",
  "types": "dist/${ARTIFACT_ID}.d.ts",
  "files": [
    "dist/**/*"
  ],
  "type": "module",${GIT_REPO_INFO}
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
${DEPENDENCIES}
  }
}
